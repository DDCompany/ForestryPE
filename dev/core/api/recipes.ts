interface ChancedRecipeItem {
    id: number;

    data?: number;

    count?: number;

    chance?: number;
}

interface RecipeItem {
    id: number;

    data?: number;

    count?: number;
}

interface SingleRecipeItem {
    id: number;

    data?: number;
}