RecipeRegistry.addBioItem({
    id: 6,
    liquidAmount: 0.25
});

RecipeRegistry.addBioItem({
    id: 81,
    liquidAmount: 0.5
});

RecipeRegistry.addBioItem({
    id: 296,
    liquidAmount: 0.5
});

RecipeRegistry.addBioItem({
    id: 338,
    liquidAmount: 0.5
});

RecipeRegistry.addBioItem({
    id: 392,
    liquidAmount: 0.5
});

RecipeRegistry.addBioItem({
    id: 39,
    liquidAmount: 0.5
});

RecipeRegistry.addBioItem({
    id: 40,
    liquidAmount: 0.5
});

RecipeRegistry.addBioItem({
    id: ItemID.rubberSapling,
    liquidAmount: 0.25
});